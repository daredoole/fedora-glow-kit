name = "full-send"
description = "Everything. Still prompts before risky third-party or destructive actions unless --yes is supplied."
sections = ["core", "extras", "desktop", "ai", "security"]
risk = "high"
